import { memo } from "react";

export function ImageList(props) {
  const {
    getDetailContainer,
    data = [],
    isMobile,
    loading,
    hasMore,
  } = props;

  return "Image List"
}

export default memo(ImageList);