// AI Chat is the chat mode of the search application (`/chat?mode=chat`) — a first-class entry
// pointing at the same fullscreen component, so the two app forms stay in lockstep.
export { Component } from '../search';
